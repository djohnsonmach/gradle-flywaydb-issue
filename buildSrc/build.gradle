repositories() {
    mavenCentral()
    maven {
        url "https://plugins.gradle.org/m2/"
    }
}

ext {
    flywayVersion = '4.0.3'
}

dependencies() {
    compile localGroovy()
    compile gradleApi()
    compile "org.flywaydb:flyway-gradle-plugin:$flywayVersion"
}

